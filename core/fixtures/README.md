# Fixtures

Put small synthetic image sets here for engine tests.

Expected outputs should be stored beside each fixture set, for example:

```text
fixtures/basic_exact/
  input/
  expected_groups.json
  expected_summary.json
```

Avoid committing personal photos.
